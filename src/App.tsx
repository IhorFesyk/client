import React from "react"

import { AppRouter } from "routes"

const App: React.FC = () => {
  return (
    <div className="w-screen h-screen">
      <AppRouter />
    </div>
  )
}

export default App
